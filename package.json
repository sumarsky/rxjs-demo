{
  "name": "rxjs-demo",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "bower install",
    "start": "./node_modules/.bin/live-server --host=localhost --port=3000 ."
  },
  "devDependencies": {
    "bower": "^1.7.9",
    "live-server": "^0.9.2"
  }
}
